<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.taskboard.backend.mapper.RoleMapper">

    <resultMap id="RoleResultMap" type="com.taskboard.backend.model.Role">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="code" column="code"/>
        <result property="description" column="description"/>
        <result property="status" column="status"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="findById" parameterType="java.lang.Long" resultMap="RoleResultMap">
        SELECT id, name, code, description, status, created_at, updated_at
        FROM roles
        WHERE id = #{id}
    </select>

    <select id="findAll" resultMap="RoleResultMap">
        SELECT id, name, code, description, status, created_at, updated_at
        FROM roles
        ORDER BY id ASC
    </select>

    <select id="findByUserId" parameterType="java.lang.Long" resultMap="RoleResultMap">
        SELECT r.id, r.name, r.code, r.description, r.status, r.created_at, r.updated_at
        FROM roles r
        INNER JOIN user_roles ur ON r.id = ur.role_id
        WHERE ur.user_id = #{userId}
    </select>

    <insert id="insert" parameterType="com.taskboard.backend.model.Role" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO roles (name, code, description, status, created_at, updated_at)
        VALUES (#{name}, #{code}, #{description}, #{status}, NOW(), NOW())
    </insert>

    <update id="update" parameterType="com.taskboard.backend.model.Role">
        UPDATE roles
        SET
            name = #{name},
            code = #{code},
            description = #{description},
            status = #{status},
            updated_at = NOW()
        WHERE id = #{id}
    </update>

    <delete id="delete" parameterType="java.lang.Long">
        DELETE FROM roles WHERE id = #{id}
    </delete>

    <delete id="deleteRolePermissions" parameterType="java.lang.Long">
        DELETE FROM role_permissions WHERE role_id = #{roleId}
    </delete>

    <insert id="insertRolePermission">
        INSERT INTO role_permissions (role_id, permission_id)
        VALUES (#{roleId}, #{permissionId})
    </insert>
    
    <delete id="deleteRoleMenus" parameterType="java.lang.Long">
        DELETE FROM role_menus WHERE role_id = #{roleId}
    </delete>
    
    <insert id="insertRoleMenu">
        INSERT INTO role_menus (role_id, menu_id)
        VALUES (#{roleId}, #{menuId})
    </insert>

</mapper>
